using System;

class Program
{
    static void Main()
    {
        int[,] answer = new int[9,9];
        int[] area_key = {0, 3, 6};
        
        int[] square_used_nums = new int[9];
        bool alreadythere = true;

        int num;
        Random rnd = new Random();
        
        // Ёти 2 цикла проход€тс€ по 9 угловым точкам (тк один большой квадрат разделЄн на 9 мелких)
        foreach(int i in area_key)
        {
            foreach(int j in area_key)
            {
                Array.Clear(square_used_nums, 0, 9); // ќчищаем список уже использованных в генерении квадрата чисел
                
                // √енераци€ отдельного квадрата (одного из дев€ти)
                for (int q = i; q < i + 3; q++)
                    {
                        for(int w = j; w < j + 3; w++)
                        {
                            
                            // √енерируем, провер€м и добавл€ем число, если оно подходит
                            // ƒл€ проверки нужно запоминать числа, использованные в квардрате, а в лини€х и колонках - нет
                            // по ним можно просто пройтись в цикле
                            int test_count = 0;
                            while(true)
                            {
                                test_count += 1;
                                Console.WriteLine(test_count);
                                
                                num = rnd.Next(1, 9);
                                alreadythere = true;
                                
                                // есть ли такое число в квадрате
                                if (Array.IndexOf(square_used_nums, num) == -1)
                                {
                                    // есть ли такое число в линии
                                    for(int x=0; x < 9; x ++)
                                    {
                                        if (answer[q, x] != num)
                                            alreadythere = false;
                                    }
                                    
                                    // если уже есть такое число в линии, то пропускаем итерацию и сразу генерируем новое число
                                    if (alreadythere == true)
                                        continue;
                                    
                                    // есть ли такое число в столбце
                                    for(int x=0; x < 9; x ++)
                                    {
                                        if (answer[x, w] != num)
                                            alreadythere = false;
                                    }
                                }
                                
                                if(alreadythere == false)
                                {
                                    answer[q,w] = num;
                                    square_used_nums[Array.IndexOf(square_used_nums, 0)] = num;
                                    break;
                                }
                            }
                        }
                    }
                
            }
        }
        
        // вывод ответа
        for (int i = 0; i<9;i++)
        {
            for (int j = 0; j<9; j++)
                Console.Write(answer[i,j] + " ");
            Console.WriteLine();
        }
    }
    
    // функци€ дл€ копировани€ целого пол€ и удалени€ определЄнных клеток
}


/*
—крипт, который генерит судоку и позвон€ет заполнить его\увидеть правильный ответ

√енерит уже правильно заполненный (ответ), не показыва€ нам
 опируем в ещЄ один двумерный массив, убираем числа в заданных област€х, показываем пользователю

 ак правильно  сгенерировать?
–андомим квадрат:
  .  аждому числу нельз€ повтор€тьс€ в квадрате
  . + нельз€ повтор€тьс€ в линии и в столбце
  . +++ при генерации нового числа нужно в цикле перезаписать числа, которые уже использованы в строке и в столбце (плюс ещЄ есть числа, которые использованы 
   в самом квадрате)  вообще, их можно объедин€ть в 
  
ѕри показе пользователю нерешЄнного судоку, можно не копировать массив, а не выводить просто определЄнные числа
*/